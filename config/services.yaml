parameters:
    app.name: 'GOSA'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    Gosa\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'
