<?php

/**
 * Returns the importmap for this application.
 *
 * - "path" is a path inside the asset mapper "paths" (i.e. "assets/")
 * - "entrypoint" (bool) indicates if the file should be treated as an entrypoint
 */
return [
    'app' => [
        'path' => './assets/app.js',
        'entrypoint' => true,
    ],
];
